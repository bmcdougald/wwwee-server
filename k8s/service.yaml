apiVersion: v1
kind: Service
metadata:
  name: wwwee-server
  labels:
    app.kubernetes.io/name: wwwee-server
spec:
  selector:
    app.kubernetes.io/name: wwwee-server
  ports:
    - name: https
      port: 443
      targetPort: https
