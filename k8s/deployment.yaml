apiVersion: apps/v1
kind: Deployment
metadata:
  name: wwwee-server
  labels:
    app.kubernetes.io/name: wwwee-server
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: wwwee-server
  template:
    metadata:
      labels:
        app.kubernetes.io/name: wwwee-server
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: wwwee-server
          image: wwwee-server:local
          imagePullPolicy: IfNotPresent
          ports:
            - name: https
              containerPort: 8443
          env:
            - name: WWWEE_ADDR
              value: ":8443"
            - name: WWWEE_CONTENT_DIR
              value: /srv/content
            # The image is built with the validated Go Cryptographic Module
            # and defaults to FIPS 140-3 mode. Keep the deployment explicit so
            # the compliance posture is visible in the workload manifest.
            - name: GODEBUG
              value: fips140=on
            - name: WWWEE_TLS_CERT_FILE
              value: /run/tls/tls.crt
            - name: WWWEE_TLS_KEY_FILE
              value: /run/tls/tls.key
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              cpu: 250m
              memory: 64Mi
          livenessProbe:
            httpGet:
              scheme: HTTPS
              path: /healthz
              port: https
            initialDelaySeconds: 3
            periodSeconds: 15
          readinessProbe:
            httpGet:
              scheme: HTTPS
              path: /readyz
              port: https
            initialDelaySeconds: 3
            periodSeconds: 10
          volumeMounts:
            - name: content
              mountPath: /srv/content
              readOnly: true
            - name: tls
              mountPath: /run/tls
              readOnly: true
            - name: tmp
              mountPath: /tmp
      volumes:
        # Site content is supplied by the publishing pipeline through the
        # shared persistent volume and is mounted read-only by the server.
        - name: content
          persistentVolumeClaim:
            claimName: wwwee-content
        - name: tls
          secret:
            secretName: wwwee-server-tls
            defaultMode: 0440
        - name: tmp
          emptyDir:
            sizeLimit: 16Mi
