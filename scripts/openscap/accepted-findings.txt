# OpenSCAP findings reviewed for this image.
#
# Format: one line per accepted finding — a DISA rule id (or unique
# substring of one) as it appears in results.xml. Everything after a '#'
# is a comment. Any "fail" result NOT listed here fails the scan.
#
# Re-review this list whenever the base image, Go toolchain, or SCAP
# content version changes.

# --- FIPS 140-3 crypto ------------------------------------------------------
# These findings were previously accepted because the image used stock Go
# crypto. The build now uses the Go Cryptographic Module v1.0.0 (CMVP #5247)
# with FIPS 140-3 mode enabled. Remove these entries after a scan confirms
# that the SCAP content recognizes the validated module.
#
# SV-203649 is the GPOS requirement for authentication TO THE CRYPTOGRAPHIC
# MODULE. It is not a requirement for this web application to implement user
# authentication or identity management. Reassess it after the FIPS scan.
SV-203739r987791_rule
SV-203776r959006_rule
SV-203649r971535_rule

# --- Confidentiality/integrity of data in transit ---------------------------
# These checks assess encryption of data in transit. The image scanned by
# `scripts/openscap-scan.sh` is not configured with
# WWWEE_TLS_CERT_FILE/WWWEE_TLS_KEY_FILE, so it serves plain HTTP. Deploy
# with TLS enabled (or terminate TLS at an ingress/load balancer) and treat
# these as pending until deployment certificates are available and verified.
SV-203750r958912_rule
SV-203751r958914_rule

# --- Organization-approved trust anchors ------------------------------------
# The image ships Debian's default system CA bundle, not an organization-
# curated trust store. Determine the required organizational trust-anchor
# model before removing this acceptance. If custom trust anchors are
# mandated, mount/replace the trust store at deployment time.
SV-263659r982563_rule
